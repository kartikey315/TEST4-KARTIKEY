generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id               String  @id
  username         String
  dob              String
  phonenumber      String
  passcode         String
  lastRequestedOTP String?
}
